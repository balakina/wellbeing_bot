# Wellbeing Diary — MCP Server (FastMCP) + CLI Agent (LangGraph)

Учебный проект личного дневника с разделением ответственности:

- **MCP‑сервер (FastMCP)** — хранение данных в **SQLite** + **FTS5 поиск по словам** + **семантический поиск FAISS** (embeddings) + опциональный **rerank** (CrossEncoder). Всё выдаётся как MCP tools.
- **CLI‑агент (LangGraph)** — диалоговый интерфейс, маршрутизация команд, генерация тегов/интерпретации/ответа через **GigaChat**, сохранение и поиск через MCP tools.

Проект демонстрирует архитектуру **Agent ↔ MCP tools ↔ Storage** и подходит как шаблон для агентных систем с выделенным tool‑сервером.

LLM (GigaChat) используется **только на стороне агента** — для понимания контекста, генерации ответа и сводок. Сервер — детерминированный слой хранения/поиска (SQLite/FTS/FAISS), что даёт масштабируемость и контроль над ресурсами.

---

## Возможности

### MCP‑сервер (`server.py`)
- SQLite таблица `entries` для хранения записей дневника
- **FTS5** индекс `entries_fts` для поиска по словам (с fallback на `LIKE`, плюс варианты `е/ё`)
- **FAISS IndexFlatIP** + `normalize_embeddings=True` → cosine similarity
- Embeddings через **multilingual E5** (по умолчанию `intfloat/multilingual-e5-small`) с префиксами `query:` / `passage:`
- Опциональный **rerank** через CrossEncoder (по умолчанию `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1`)
- Health‑check endpoint: `GET /health`
- MCP resource: `wellbeing://status`
- Unicode‑safe persist FAISS (важно для Windows и путей с кириллицей)

### CLI‑агент (`agent.py`)
- Ввод записи и сохранение по схеме: **текст → оценка mood 1–5**
- Автогенерация:
  - `tags` (2–5 слов)
  - `interpretation` (1–2 предложения)
  - `reply` (поддержка + маленький шаг)
  - `question` (1 короткий вопрос)
- Команды:
  - `сводка` / `итог` / `резюме` / `YYYY-MM-DD` / `сегодня` → сводка за день
  - `найди <слово/фраза>` → **поиск по словам** (FTS5/LIKE)
  - `найди! <запрос>` → **смысловой поиск** (FAISS → SQLite → rerank, если включён)
  - `paths` / `debug` / `status` → диагностическая информация (пути, ntotal, размеры)
  - `reindex` / `реиндекс` / `переиндекс` → пересобрать FAISS (и FTS) из SQLite
  - `report` / `отчет` / `график` → PNG график настроения за 4 недели (если есть matplotlib)
  - `выход` / `exit` → выйти

---

## Архитектура

```
User (CLI)
   ↓
LangGraph Agent (GigaChat)
   ↓
MCP Client (tools)
   ↓
FastMCP Server
   ↓
SQLite (wellbeing.db) + FTS5 + FAISS (faiss.index + faiss_map.json)
```

---

## Структура проекта

```
.
├── server.py            # MCP сервер (FastMCP + SQLite + FTS5 + FAISS + rerank)
├── agent.py             # CLI агент (LangGraph + GigaChat)
├── wellbeing.db         # SQLite база (создаётся автоматически)
├── faiss.index          # FAISS индекс (создаётся автоматически)
├── faiss_map.json       # соответствие faiss_id -> entry_id (создаётся автоматически)
├── reports/             # PNG отчёты (создаётся автоматически)
├── .env                 # локальные переменные окружения (не коммитить)
└── README.md
```

---

## MCP tools (сервер)

Сервер экспортирует следующие tools (актуально для `server.py`):

- `log_entry(raw_text, mood_score, tags="", interpretation="")` — сохранить запись (и добавить в FAISS)
- `get_daily_summary(date="YYYY-MM-DD")` — записи за день + статистика (count/avg mood)
- `search_word(query, limit=20)` — поиск по словам через FTS5 (с prefix‑вариантами), fallback на `LIKE`, учёт `е/ё`
- `search_semantic_only(query, top_k=20)` — FAISS → SQLite (без rerank)
- `search_with_rerank(query, top_k=50, top_n=8)` — FAISS → SQLite → rerank (если включён)
- `debug_paths()` — диагностика путей + `faiss_ntotal`/`map_len`/`embed_dim`
- `rebuild_faiss_from_db(batch_size=256)` — полностью пересобрать FAISS из SQLite
- `rebuild_fts_from_db()` — пересобрать FTS5 из SQLite
- `export_last_weeks_report(weeks=4, out_dir="reports")` — PNG отчёт (если установлен matplotlib)

MCP resource:

- `wellbeing://status` — JSON со статусом, количеством записей и размером индекса

---

## Требования

- Python **3.10+**
- Доступ к **GigaChat** (только для агента)
- Библиотеки:
  - `fastmcp`, `starlette`, `aiosqlite`, `python-dotenv`
  - `langgraph`, `langchain-mcp-adapters`, `langchain-gigachat`
  - `sentence-transformers`, `faiss-cpu`, `numpy`
  - (опционально) `matplotlib` — для `report`

> На Windows чаще проще ставится `faiss-cpu` (а не GPU‑варианты).

---

## Установка

### 1) Виртуальное окружение

**Windows (PowerShell)**
```bash
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

**macOS / Linux**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### 2) Зависимости

Если используешь `requirements.txt`:
```bash
pip install -r requirements.txt
```

Или вручную:
```bash
pip install fastmcp starlette aiosqlite python-dotenv \
            langgraph langchain-mcp-adapters langchain-gigachat \
            sentence-transformers faiss-cpu numpy matplotlib
```

---

## Конфигурация (.env)

Создай `.env` в корне проекта.

### Минимально для агента + сервера
```env
# Agent -> Server
WELLBEING_MCP_URL=http://127.0.0.1:8100/mcp

# GigaChat (agent)
GIGACHAT_CREDENTIALS=PASTE_YOUR_CREDENTIALS_HERE
GIGACHAT_VERIFY_SSL=false
GIGACHAT_SCOPE=GIGACHAT_API_PERS
```

### (Опционально) Параметры сервера / поиска
```env
# Storage
WELLBEING_DB_PATH=./wellbeing.db

# FAISS
WELLBEING_FAISS_INDEX=./faiss.index
WELLBEING_FAISS_MAP=./faiss_map.json

# Embeddings / rerank
WELLBEING_EMBED_MODEL=intfloat/multilingual-e5-small
WELLBEING_RERANK_MODEL=cross-encoder/mmarco-mMiniLMv2-L12-H384-v1
WELLBEING_ENABLE_RERANK=true

# Server
WELLBEING_HOST=0.0.0.0
WELLBEING_PORT=8100
WELLBEING_PATH=/mcp

# Offline mode (если модели уже скачаны в кэш)
WELLBEING_OFFLINE=true
```

⚠️ `.env` не должен попадать в репозиторий (добавь в `.gitignore`).

---

## Запуск

### 1) Запуск MCP‑сервера

```bash
python server.py
```

Ожидаемый вывод:
- `✅ DB initialized (entries + FTS5)`
- `✅ FAISS loaded. ntotal = ...`
- `🚀 http://127.0.0.1:8100/mcp`

Проверка health:
- `GET http://127.0.0.1:8100/health` → `OK`

### 2) Запуск CLI‑агента

```bash
python agent.py
```

Пример:
```
Ты: Сегодня было тревожно, но я всё равно сделал важное.
Ты: 3
```

Поиск:
```
Ты: найди тревога
Ты: найди! конфликт с коллегой
```

Сводка:
```
Ты: сводка
Ты: 2026-01-27
Ты: сегодня
```

Пересборка индекса:
```
Ты: reindex
```

---

## Хранение данных

### SQLite: `wellbeing.db`

Таблица `entries`:

| Поле | Тип | Описание |
|---|---|---|
| id | INTEGER | первичный ключ |
| created_at | TEXT | ISO timestamp (`YYYY-MM-DDTHH:MM:SS`) |
| raw_text | TEXT | текст записи |
| mood_score | INTEGER | оценка 1–5 |
| tags | TEXT | теги (строка) |
| interpretation | TEXT | интерпретация |

### FTS5: `entries_fts`
- обновляется триггерами при insert/update/delete
- можно принудительно пересобрать: `rebuild_fts_from_db()`

### FAISS: `faiss.index` + `faiss_map.json`
- `faiss.index` хранит вектора `passage:` для `raw_text`
- `faiss_map.json` хранит соответствие: **позиция в индексе → entry_id в SQLite**
- если индекс/мапа битые — сервер переименует их в `*.broken.TIMESTAMP` и создаст новые
- индекс можно всегда пересобрать: команда агента `reindex`

---

## Примечания

- Если переносишь проект между машинами, **SQLite** стоит коммитить/копировать только если это нужно. FAISS можно всегда пересобрать.
- Команда `paths` помогает быстро проверить, что агент и сервер смотрят на одни и те же файлы.
- Если `WELLBEING_OFFLINE=true`, HuggingFace будет работать в offline‑режиме — убедись, что модели уже скачаны в кэш.

---

## Идеи улучшений

- Экспорт в CSV/JSON
- Web UI (FastAPI + фронтенд)
- Фильтры поиска по дате/тегам
- Периодная статистика настроения
- Шифрование базы / локальный password‑lock
